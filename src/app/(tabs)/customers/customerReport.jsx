import { View, Text, ScrollView } from 'react-native';
import React from 'react';

const customerReport = () => {
    return (
        <ScrollView className="p-2">
            <Text>customer reports in progress</Text>
        </ScrollView>
    );
};

export default customerReport;